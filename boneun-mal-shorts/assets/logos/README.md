# assets/logos

Brand endings render from `config/brand.json` typography by default, so no
image file is required. Drop `wordmark.png` (transparent, ≤ 700px wide) here to
use a real wordmark instead — the renderer picks it up automatically and keeps
it inside the safe area, never over the signing space.
